# Supabase project settings → API
# The service role key is only ever used server-side (route handlers).
# It is NOT prefixed with NEXT_PUBLIC_ so Next.js never ships it to the browser.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
